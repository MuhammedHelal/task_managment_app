import 'package:flutter/material.dart';

class TasksHistoryView extends StatelessWidget {
  const TasksHistoryView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
